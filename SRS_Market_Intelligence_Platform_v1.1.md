# وثيقة المتطلبات والمعمارية الكاملة
## منصة الذكاء التحليلي للأسهم والأوبشن الأمريكية
### AI Decision Intelligence Platform — SRS & Architecture Document

**الإصدار:** 1.0 (للاعتماد)
**التاريخ:** يوليو 2026
**الحالة:** بانتظار اعتماد المالك قبل بدء أي تنفيذ برمجي
**اللغة:** عربي مع المصطلحات التقنية بالإنجليزية

---

# 1. رؤية المنتج — Product Vision

## 1.1 ما هو المنتج

منصة **دعم قرار تحليلية** (Decision Support System) للأسهم والأوبشن في السوق الأمريكي. المستخدم يُدخل رمز سهم (مثل NVDA) فيحصل خلال ثوانٍ على تقرير تحليلي احترافي مبني على حسابات رياضية حتمية (Deterministic) مع تفسير لغوي واضح يولّده نموذج لغوي (LLM).

## 1.2 ما هو ليس المنتج

- ليس بوت تداول آلي — لا ينفّذ أي صفقة.
- ليس خدمة توصيات أو إشارات — المخرجات تحليل تعليمي ومستويات فنية ملحوظة.
- ليس متنبئاً — لا يعد بأرباح ولا يدّعي اليقين، ويصرّح بعدم اليقين دائماً.
- القرار النهائي دائماً للمستخدم.

## 1.3 المبادئ الحاكمة (غير قابلة للتفاوض)

| # | المبدأ | التطبيق العملي |
|---|--------|----------------|
| P1 | الرياضيات للحساب، والـ LLM للتفسير فقط | لا يُطلب من أي نموذج لغوي حساب مؤشر أو درجة أبداً |
| P2 | كل درجة (Score) من معادلة شفافة معلنة | لا أرقام "ديكورية" مولّدة من AI |
| P3 | قابلية التفسير Explainability | كل استنتاج يُرجع لمدخلاته الرقمية |
| P4 | التكلفة تحت السيطرة دائماً | Kill-Switch + سقف يومي + كاش |
| P5 | لا ارتباط بمزوّد واحد | Adapter Pattern للبيانات وللـ LLM معاً |
| P6 | كل تحليل يُحفظ ويُدقّق لاحقاً | Self-Audit إلزامي معمارياً |
| P7 | الصياغة القانونية الآمنة | تحليل تعليمي، لا توصيات مباشرة |

## 1.4 المستخدمون المستهدفون

- **المرحلة 1:** المالك فقط (استخدام شخصي).
- **المرحلة 2 (محتملة):** مجموعة صغيرة مغلقة بدعوات — يستلزم تفعيل كامل لطبقة إخلاء المسؤولية والمصادقة متعددة المستخدمين.

## 1.5 مقياس نجاح المنتج

المنصة ناجحة إذا: (أ) تقرير السهم يصدر في أقل من 15 ثانية، (ب) تكلفة التقرير الواحد أقل من 0.05 دولار، (ج) نظام Self-Audit يعمل ويبني سجل دقة تاريخي، (د) لا يحدث أي تجاوز غير مقصود لسقف التكلفة الشهري.

---

# 2. مواصفات المتطلبات البرمجية — SRS

## 2.1 المتطلبات الوظيفية (Functional Requirements)

### FR-1: إدخال الرمز وتوليد التقرير
- **FR-1.1** يقبل النظام رمز سهم أمريكي (Ticker) نصياً: NVDA, TSLA, AAPL...
- **FR-1.2** يتحقق النظام من صحة الرمز قبل أي استدعاء مدفوع.
- **FR-1.3** يولّد النظام تقريراً كاملاً خلال ≤ 15 ثانية (هدف: ≤ 10 ثوانٍ).
- **FR-1.4** إذا كان هناك تحليل حديث لنفس الرمز داخل نافذة الكاش، يُعاد نفس التقرير بدون أي استدعاء خارجي جديد.

### FR-2: محرك التحليل الحتمي (Deterministic Engine)
- **FR-2.1** حساب المؤشرات بالكود الرياضي فقط: RSI, MACD, VWAP, ATR, EMA/SMA (20/50/200), Bollinger Bands.
- **FR-2.2** كشف الدعوم والمقاومات خوارزمياً (Pivot Points + Local Extrema Clustering).
- **FR-2.3** حساب التقلب: Historical Volatility (20/60 يوم) + ATR%.
- **FR-2.4** تقييم السيولة: متوسط الحجم، الحجم النسبي (RVOL)، الـ Dollar Volume.
- **FR-2.5** تصنيف حالة السوق: Trending Up / Trending Down / Ranging / High Volatility — بمعايير رقمية معلنة.
- **FR-2.6** حساب مستوى المخاطرة من معادلة (القسم 12).
- **FR-2.7** إخراج كل ما سبق كـ JSON منظّم وموثّق بـ Schema ثابت.

### FR-3: محرك التقرير اللغوي (LLM Report Engine)
- **FR-3.1** استدعاء LLM واحد رئيسي يستلم JSON الحتمي ويولّد التقرير.
- **FR-3.2** التقرير يشمل: ملخص الحالة، قراءة المؤشرات، المستويات الفنية الملحوظة، سيناريو صاعد/هابط/محايد، المخاطر الرئيسية، ولماذا وصل النظام لهذه القراءة.
- **FR-3.3** فقرة Devil's Advocate إلزامية داخل كل تقرير (القسم 14).
- **FR-3.4** يُمنع الـ LLM (عبر الـ Prompt والـ Validation) من توليد أرقام درجات جديدة — يستخدم فقط الأرقام الواردة في الـ JSON.
- **FR-3.5** التقرير باللغة العربية افتراضياً مع خيار الإنجليزية.

### FR-4: الحفظ والتدقيق الذاتي
- **FR-4.1** كل تحليل يُحفظ كاملاً: المدخلات، JSON الحتمي، التقرير، التكلفة، الوقت.
- **FR-4.2** تُحفظ المستويات القابلة للتحقق (Entry Zone, Invalidation, Targets كمستويات فنية) لأغراض التدقيق.
- **FR-4.3** MVP يتضمن Placeholder جاهزاً معمارياً لوظيفة التدقيق المجدولة (القسم 15).

### FR-5: التحكم بالتكلفة
- **FR-5.1** سقف تكلفة يومي وشهري قابل للضبط (افتراضي: 1$/يوم، 20$/شهر).
- **FR-5.2** عند بلوغ السقف يتوقف النظام عن الاستدعاءات المدفوعة (Kill-Switch) ويعرض رسالة واضحة.
- **FR-5.3** عدّاد تكلفة لحظي ظاهر في الواجهة.
- **FR-5.4** كل استدعاء خارجي (بيانات أو LLM) يُسجّل بتكلفته التقديرية قبل التنفيذ وتكلفته الفعلية بعده.

### FR-6: محرك الأوبشن (المرحلة 2 — بعد استقرار محرك الأسهم)
- **FR-6.1** جلب سلسلة الأوبشن وحساب الـ Greeks (Delta, Gamma, Theta, Vega) بمكتبة تسعير (Black-Scholes) عند غيابها من المزود.
- **FR-6.2** حساب IV Rank/Percentile، الـ Spread%، Expected Move، وترتيب العقود بمعادلة جودة معلنة.
- **FR-6.3** شرح "لماذا هذا العقد أقوى من ذاك" بالأرقام أولاً ثم التفسير اللغوي.

### FR-7: خارج نطاق MVP (مؤجّل صراحة)
تحليل الصور/السكرينشوت، الأخبار والمشاعر، Options Flow اللحظي، Dark Pool، Insider Activity، تعدد المستخدمين الكامل.

## 2.2 المتطلبات غير الوظيفية (Non-Functional Requirements)

| المعرّف | المتطلب | القيمة المستهدفة |
|---------|---------|------------------|
| NFR-1 | زمن التقرير (بدون كاش) | ≤ 15 ثانية |
| NFR-2 | زمن التقرير (من الكاش) | ≤ 1 ثانية |
| NFR-3 | تكلفة التقرير الواحد | ≤ 0.05$ (هدف: ~0.02$) |
| NFR-4 | التكلفة الشهرية الكلية للمرحلة الأولى | ≤ 25$ (استضافة + بيانات + LLM) |
| NFR-5 | قابلية استبدال مزوّد البيانات | بدون تعديل أي كود خارج الـ Adapter |
| NFR-6 | قابلية استبدال مزوّد الـ LLM | Anthropic ⇄ OpenAI بتغيير إعداد واحد |
| NFR-7 | الدقة الحسابية للمؤشرات | مطابقة قيم TradingView ضمن هامش تقريب |
| NFR-8 | التوفر | مقبول توقف قصير (مشروع شخصي، لا SLA) |
| NFR-9 | الأمان | مفاتيح API في متغيرات بيئة، لا تُحفظ في الكود أو قاعدة البيانات |

---

# 3. المعمارية العامة — System Architecture

## 3.1 المخطط الطبقي

```
┌──────────────────────────────────────────────────────────┐
│                    Frontend (Web UI)                     │
│        إدخال الرمز · عرض التقرير · عدّاد التكلفة         │
└──────────────────────────┬───────────────────────────────┘
                           │ REST (HTTPS + API Key)
┌──────────────────────────▼───────────────────────────────┐
│                  API Layer — FastAPI                     │
│   Validation · Rate Limiting · Auth · Cost Gate (قبل)   │
└───────┬──────────────────┬──────────────────┬────────────┘
        │                  │                  │
┌───────▼───────┐  ┌───────▼────────┐  ┌──────▼─────────┐
│ Cache Layer   │  │ Orchestrator   │  │ Cost Control   │
│ (Redis/SQLite)│  │ (منسّق التحليل)│  │ Service        │
└───────────────┘  └───────┬────────┘  └────────────────┘
                           │
        ┌──────────────────┼──────────────────────┐
        │                  │                      │
┌───────▼────────┐ ┌───────▼─────────┐  ┌─────────▼────────┐
│ Market Data    │ │ Deterministic   │  │ LLM Report       │
│ Adapter Layer  │ │ Analysis Engine │  │ Engine (Adapter) │
│ (Provider A/B) │ │ (Python فقط)    │  │ Anthropic/OpenAI │
└────────────────┘ └───────┬─────────┘  └─────────┬────────┘
                           │ JSON منظّم            │
                           └──────────┬───────────┘
                                      │
                           ┌──────────▼───────────┐
                           │ PostgreSQL Database  │
                           │ تحليلات · تكاليف ·   │
                           │ Self-Audit Records   │
                           └──────────────────────┘
```

## 3.2 القرارات المعمارية الجوهرية

| القرار | الاختيار | السبب |
|--------|----------|-------|
| AD-1 | Monolith منظّم (وليس Microservices) | مشروع شخصي؛ الخدمات المصغّرة تعقيد وتكلفة بلا مبرر في هذه المرحلة. البنية الداخلية معيارية (Modules) بحيث يسهل الفصل لاحقاً |
| AD-2 | طبقة حتمية أولاً، LLM أخيراً | خفض التكلفة 85%+ ومنع الهلوسة الرقمية |
| AD-3 | Adapter Pattern مزدوج (Data + LLM) | متطلب صريح بعدم الارتباط بمزوّد |
| AD-4 | Cost Gate قبل كل استدعاء مدفوع (وليس بعده) | المنع خير من التسجيل؛ درس Replit |
| AD-5 | PostgreSQL من اليوم الأول | Self-Audit يحتاج بيانات تاريخية منظمة؛ SQLite بديل مقبول للتطوير المحلي |
| AD-6 | التقرير يُبنى من JSON موقّع بـ Schema | يضمن أن الـ LLM لا يخترع أرقاماً (Validation بعد التوليد) |

---

# 4. تدفق البيانات — Data Flow

## 4.1 التدفق الرئيسي: طلب تحليل سهم

```
[1] المستخدم يدخل "NVDA"
        │
[2] API Layer: تحقق من صحة الرمز (Regex + قائمة رموز معروفة محلية)
        │
[3] Cache Check: هل يوجد تحليل ≤ 15 دقيقة (خلال السوق) أو ≤ 12 ساعة (السوق مغلق)؟
        │── نعم → إرجاع التقرير المحفوظ فوراً (تكلفة = 0)
        │── لا  ↓
[4] Cost Gate: هل التكلفة التقديرية للطلب + المصروف اليومي ≤ السقف؟
        │── لا  → رفض واضح برسالة "بلغت السقف اليومي" (Kill-Switch)
        │── نعم ↓
[5] Market Data Adapter: جلب OHLCV يومي (سنة) + Intraday حسب التوفر
        │
[6] Deterministic Engine: حساب كل المؤشرات والدرجات → JSON منظّم
        │   (صفر تكلفة خارجية — Python خالص)
[7] حفظ الـ JSON في قاعدة البيانات (حتى لو فشل الـ LLM لاحقاً)
        │
[8] LLM Report Engine: استدعاء واحد → التقرير + فقرة Devil's Advocate
        │
[9] Validation: فحص أن كل الأرقام في التقرير موجودة في الـ JSON المصدر
        │── فشل → إعادة توليد مرة واحدة، وإلا إرجاع الـ JSON مع تقرير مبسّط قالبي
[10] تسجيل التكلفة الفعلية + حفظ التقرير + إنشاء سجل Self-Audit معلّق
        │
[11] إرجاع التقرير للمستخدم + تحديث عدّاد التكلفة في الواجهة
```

## 4.2 تدفق التدقيق الذاتي (مجدول — Placeholder في MVP)

```
[يومياً 01:00 UTC] Scheduled Job
   → اجلب سجلات Self-Audit المعلّقة التي مضى عليها 5 و10 و20 جلسة تداول
   → اجلب السعر الفعلي الحالي/التاريخي للرمز
   → قارن: هل لُمست المستويات؟ أي سيناريو تحقق؟
   → احسب Outcome Score واحفظه
   → حدّث سجل الدقة التراكمي لكل نوع حالة سوق
```

---

# 5. معمارية الخلفية — Backend Architecture

## 5.1 التقنيات

| المكوّن | الاختيار | البديل | سبب الاختيار |
|---------|----------|--------|---------------|
| اللغة | Python 3.12 | — | مكتبات مالية ناضجة (pandas, numpy, py_vollib) |
| إطار الـ API | FastAPI | Flask | أداء + توثيق تلقائي (OpenAPI) + Validation مدمج بـ Pydantic |
| قاعدة البيانات | PostgreSQL 16 | SQLite (تطوير محلي) | JSONB لحفظ التحليلات + موثوقية |
| الكاش | Redis (أو جدول كاش في Postgres للتوفير) | — | انظر القسم 17 |
| المهام المجدولة | APScheduler داخل التطبيق | Render Cron Jobs | بساطة؛ لا حاجة لـ Celery في هذا الحجم |
| ORM | SQLAlchemy 2.x | — | معيار الصناعة |
| الحسابات المالية | pandas + numpy + مكتبة مؤشرات (pandas-ta) + py_vollib للـ Greeks | حساب يدوي موثّق | سرعة تطوير مع إمكانية التحقق |

## 5.2 بنية المجلدات (Modular Monolith)

```
app/
├── main.py                  # نقطة الدخول FastAPI
├── config.py                # الإعدادات (Pydantic Settings من env)
├── api/
│   ├── routes_analysis.py   # POST /analyze, GET /report/{id}
│   ├── routes_audit.py      # GET /audit/summary, /audit/{symbol}
│   ├── routes_cost.py       # GET /cost/today, /cost/month
│   └── deps.py              # Auth, Rate limit dependencies
├── core/
│   ├── orchestrator.py      # منسّق التدفق الكامل (القسم 4.1)
│   ├── cost_gate.py         # بوابة التكلفة + Kill-Switch
│   └── cache.py             # منطق الكاش
├── engines/
│   ├── deterministic/
│   │   ├── indicators.py    # RSI, MACD, VWAP, ATR, MAs, BB
│   │   ├── levels.py        # دعوم/مقاومات
│   │   ├── volatility.py    # HV, ATR%
│   │   ├── liquidity.py     # حجم، RVOL، Dollar Volume
│   │   ├── regime.py        # تصنيف حالة السوق
│   │   └── scoring.py       # كل معادلات الدرجات (القسم 12)
│   ├── options/             # (المرحلة 2 — فارغ في MVP مع واجهات معرّفة)
│   │   ├── greeks.py
│   │   ├── iv_metrics.py
│   │   └── contract_ranker.py
│   └── llm/
│       ├── report_engine.py     # بناء الـ Prompt + التوليد + Validation
│       ├── devils_advocate.py   # منطق فقرة المراجعة النقدية
│       └── adapters/
│           ├── base.py          # LLMAdapter (واجهة مجردة)
│           ├── anthropic_llm.py
│           └── openai_llm.py
├── providers/
│   ├── base.py              # MarketDataAdapter (واجهة مجردة)
│   ├── provider_a.py        # المزوّد الأساسي
│   └── provider_b.py        # المزوّد الاحتياطي
├── db/
│   ├── models.py            # جداول SQLAlchemy
│   └── repository.py        # عمليات القراءة/الكتابة
├── audit/
│   └── self_audit.py        # وظيفة التدقيق المجدولة (Placeholder في MVP)
└── legal/
    └── disclaimers.py       # نصوص إخلاء المسؤولية المعتمدة
```

## 5.3 الواجهات المجردة (العقود المعمارية)

**عقد مزوّد البيانات — أي مزوّد جديد يطبّق هذه الواجهة فقط:**

```
MarketDataAdapter (Abstract):
    get_daily_ohlcv(symbol, lookback_days) -> DataFrame
    get_intraday(symbol, interval)         -> DataFrame | None
    get_quote(symbol)                      -> Quote
    get_options_chain(symbol, expiry)      -> OptionsChain   # المرحلة 2
    estimated_cost_per_call()              -> float
    is_market_open()                       -> bool
```

**عقد الـ LLM — التبديل بين Anthropic وOpenAI بتغيير `LLM_PROVIDER` في env:**

```
LLMAdapter (Abstract):
    generate(system_prompt, user_content, max_tokens) -> LLMResponse
    estimate_cost(input_tokens, max_output_tokens)    -> float
    count_tokens(text)                                -> int
```

---

# 6. معمارية الواجهة — Frontend Architecture

## 6.1 الفلسفة: أبسط ما يمكن في MVP

واجهة صفحة واحدة (SPA خفيفة أو حتى Server-Rendered من FastAPI + قوالب). لا حاجة لـ Next.js أو بناء معقد في المرحلة الأولى.

| العنصر | المواصفة |
|--------|----------|
| التقنية | React (Vite) خفيف، أو Jinja2 templates من FastAPI — القرار النهائي عند التنفيذ حسب تفضيل المالك |
| الاتجاه | RTL أصيل من البداية للعربية (درس مستفاد سابق)، مع دعم LTR للتقرير الإنجليزي |
| الخط | Tajawal أو IBM Plex Sans Arabic |
| المكونات | حقل إدخال الرمز · زر تحليل · بطاقة التقرير · شريط الدرجات (Scores) · عدّاد التكلفة اليومي · شارة "من الكاش" عند الإرجاع المخزّن · إخلاء المسؤولية الثابت أسفل كل تقرير |

## 6.2 عرض التقرير

- الدرجات تُعرض كأشرطة ملونة مع **إظهار المعادلة عند التحويم/الضغط** (تحقيق مبدأ الشفافية بصرياً).
- المستويات الفنية تُعرض بعنوان "مستويات فنية ملحوظة" وليس "نقاط دخول" (الطبقة القانونية، القسم 19).
- زر "لماذا؟" بجانب كل استنتاج يعرض المدخلات الرقمية التي بُني عليها.

---

# 7. تصميم قاعدة البيانات — Database Design

## 7.1 المخطط (PostgreSQL)

```sql
-- جدول التحليلات (القلب)
CREATE TABLE analyses (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    symbol           VARCHAR(10) NOT NULL,
    requested_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    market_open      BOOLEAN NOT NULL,
    data_provider    VARCHAR(30) NOT NULL,
    llm_provider     VARCHAR(30),              -- NULL إذا فشل/تخطى
    deterministic_json JSONB NOT NULL,          -- مخرجات المحرك الحتمي كاملة
    scores           JSONB NOT NULL,            -- الدرجات النهائية {technical: .., risk: ..}
    regime           VARCHAR(20) NOT NULL,      -- trending_up / trending_down / ranging / high_vol
    report_text_ar   TEXT,
    report_text_en   TEXT,
    llm_input_tokens  INT DEFAULT 0,
    llm_output_tokens INT DEFAULT 0,
    total_cost_usd   NUMERIC(8,5) NOT NULL DEFAULT 0,
    from_cache       BOOLEAN NOT NULL DEFAULT FALSE,
    cache_source_id  UUID REFERENCES analyses(id),
    status           VARCHAR(15) NOT NULL       -- completed / llm_failed / data_failed
);
CREATE INDEX idx_analyses_symbol_time ON analyses(symbol, requested_at DESC);

-- المستويات القابلة للتدقيق (تُستخرج من التحليل وقت إنشائه)
CREATE TABLE audit_targets (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    analysis_id      UUID NOT NULL REFERENCES analyses(id),
    symbol           VARCHAR(10) NOT NULL,
    price_at_analysis NUMERIC(12,4) NOT NULL,
    level_type       VARCHAR(20) NOT NULL,      -- support / resistance / invalidation / zone_high / zone_low
    level_value      NUMERIC(12,4) NOT NULL,
    scenario         VARCHAR(10),               -- bullish / bearish / neutral
    created_at       TIMESTAMPTZ DEFAULT now()
);

-- نتائج التدقيق الذاتي
CREATE TABLE audit_results (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    analysis_id      UUID NOT NULL REFERENCES analyses(id),
    checked_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
    horizon_days     INT NOT NULL,              -- 5 / 10 / 20 جلسة
    price_at_check   NUMERIC(12,4) NOT NULL,
    max_high_since   NUMERIC(12,4) NOT NULL,
    min_low_since    NUMERIC(12,4) NOT NULL,
    scenario_realized VARCHAR(10),              -- bullish / bearish / neutral / mixed
    levels_touched   JSONB,                     -- أي مستويات لُمست ومتى
    outcome_score    NUMERIC(4,3),              -- 0.000 – 1.000 (القسم 15)
    notes            TEXT
);

-- سجل التكاليف (كل استدعاء خارجي)
CREATE TABLE cost_ledger (
    id               BIGSERIAL PRIMARY KEY,
    occurred_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    category         VARCHAR(15) NOT NULL,      -- market_data / llm
    provider         VARCHAR(30) NOT NULL,
    analysis_id      UUID REFERENCES analyses(id),
    estimated_cost   NUMERIC(8,5) NOT NULL,     -- قبل التنفيذ
    actual_cost      NUMERIC(8,5),              -- بعد التنفيذ
    tokens_in        INT,
    tokens_out       INT
);
CREATE INDEX idx_cost_ledger_time ON cost_ledger(occurred_at);

-- إعدادات التحكم بالتكلفة (قابلة للتعديل بدون نشر جديد)
CREATE TABLE cost_limits (
    id               SMALLINT PRIMARY KEY DEFAULT 1,
    daily_cap_usd    NUMERIC(8,2) NOT NULL DEFAULT 1.00,
    monthly_cap_usd  NUMERIC(8,2) NOT NULL DEFAULT 20.00,
    kill_switch_on   BOOLEAN NOT NULL DEFAULT FALSE,  -- إيقاف يدوي فوري
    updated_at       TIMESTAMPTZ DEFAULT now()
);

-- سجل دقة تراكمي (يُحدّثه الـ Self-Audit)
CREATE TABLE accuracy_stats (
    regime           VARCHAR(20) NOT NULL,
    horizon_days     INT NOT NULL,
    total_audited    INT NOT NULL DEFAULT 0,
    avg_outcome      NUMERIC(4,3),
    last_updated     TIMESTAMPTZ,
    PRIMARY KEY (regime, horizon_days)
);
```

## 7.2 ملاحظات تصميمية

- `deterministic_json` بصيغة JSONB يسمح بالاستعلام داخل التحليلات القديمة لاحقاً (مثلاً: "كل التحليلات التي كان فيها RSI > 70").
- فصل `audit_targets` عن `audit_results` يسمح بتدقيق نفس التحليل على 3 آفاق زمنية (5/10/20 جلسة).
- `cost_ledger` يسجّل التكلفة **التقديرية قبل** التنفيذ — هذا ما يسمح للـ Cost Gate بالعمل الوقائي.

---

# 8. واجهات API — Endpoints

كل النقاط تتطلب Header: `X-API-Key` (القسم 18).

| Method | المسار | الوصف | الاستجابة |
|--------|--------|-------|-----------|
| POST | `/api/v1/analyze` | Body: `{"symbol": "NVDA", "lang": "ar"}` — يشغّل التدفق الكامل | `202` أو `200` مع التقرير، أو `429` عند بلوغ السقف |
| GET | `/api/v1/report/{analysis_id}` | جلب تقرير محفوظ | التقرير كاملاً + الدرجات + المعادلات |
| GET | `/api/v1/history?symbol=NVDA&limit=20` | آخر التحليلات لرمز | قائمة مختصرة |
| GET | `/api/v1/cost/today` | المصروف اليوم + النسبة من السقف | `{"spent": 0.23, "cap": 1.00, "pct": 23}` |
| GET | `/api/v1/cost/month` | المصروف الشهري | مماثل |
| POST | `/api/v1/cost/limits` | تعديل السقوف أو تفعيل Kill-Switch يدوياً | الإعدادات الجديدة |
| GET | `/api/v1/audit/summary` | سجل الدقة التراكمي حسب حالة السوق والأفق | جدول accuracy_stats |
| GET | `/api/v1/audit/{symbol}` | نتائج تدقيق رمز معيّن | قائمة audit_results |
| GET | `/api/v1/health` | فحص صحة النظام + حالة المزودات | حالة كل Adapter |

**عقد الاستجابة الرئيسي لـ `/analyze` (مختصر):**

```json
{
  "analysis_id": "uuid",
  "symbol": "NVDA",
  "from_cache": false,
  "regime": "trending_up",
  "scores": {
    "technical": 68, "volatility": 45, "liquidity": 92,
    "risk": 38, "overall_confidence": 64
  },
  "score_formulas_ref": "/docs/scoring",
  "notable_levels": {
    "supports": [128.4, 121.9], "resistances": [141.2, 149.8],
    "invalidation": 118.5
  },
  "report_ar": "...",
  "devils_advocate_ar": "...",
  "disclaimer": "...",
  "cost_usd": 0.021
}
```

---

# 9. استراتيجية مزودات البيانات — Data Providers Strategy

## 9.1 المبادئ

1. **لا ارتباط بمزوّد** — كل مزوّد خلف Adapter موحّد (القسم 5.3).
2. **البدء بالأرخص الكافي** — MVP يحتاج بيانات يومية + مؤجلة 15 دقيقة فقط؛ البيانات اللحظية الكاملة مؤجلة حتى إثبات الجدوى.
3. **مزوّد أساسي + احتياطي** — عند فشل الأساسي يتحول النظام تلقائياً مع تسجيل الحدث.

## 9.2 خارطة الاحتياجات حسب المرحلة

| المرحلة | البيانات المطلوبة | مستوى الكلفة المستهدف |
|---------|-------------------|------------------------|
| MVP (أسهم) | OHLCV يومي (سنة+) · Intraday مؤجل · Quote | مجاني إلى ~10$/شهر |
| المرحلة 2 (أوبشن) | سلاسل أوبشن مؤجلة · IV · OI · Volume | ~10–30$/شهر |
| المرحلة 3 | أخبار · تقويم أرباح · بيانات لحظية | حسب الجدوى المثبتة من Self-Audit |

## 9.3 معايير اختيار المزوّد (تُقيَّم وقت التنفيذ بالأسعار الحالية)

الأسعار تتغير، لذلك الوثيقة تثبّت **المعايير** لا الأسماء: (أ) تكلفة شهرية ضمن السقف أعلاه، (ب) حدود استدعاءات تكفي لـ 50 تحليلاً يومياً مع الكاش، (ج) تغطية سلاسل الأوبشن للمرحلة 2 من نفس المزوّد أو مزوّد مكمّل، (د) توثيق جيد ومكتبة Python رسمية أو REST نظيف. مرشحون معروفون بهذه الفئة: Tradier، Polygon (الخطط الأساسية)، Alpaca Market Data، yfinance كاحتياطي مجاني غير مضمون (للتطوير فقط، لا للاعتماد الإنتاجي).

## 9.4 قواعد صارمة

- المزوّد الاحتياطي المجاني لا يُستخدم للتدقيق الذاتي التاريخي إن اختلفت جودة بياناته — التدقيق يعتمد نفس مصدر التحليل الأصلي أو مصدراً موثوقاً معلناً.
- أي بيانات مؤجلة تُعلَّم في التقرير بوضوح: "بيانات مؤجلة 15 دقيقة".

---

# 10. محرك التحليل الحتمي — Deterministic Analysis Engine

**القاعدة المؤسِّسة: كل ما في هذا القسم يُحسب بـ Python الرياضي فقط. لا يلمس أي LLM أي رقم هنا.**

## 10.1 المؤشرات (indicators.py)

| المؤشر | المعطيات | ملاحظات التنفيذ |
|--------|----------|------------------|
| RSI | 14 فترة، إغلاق يومي | Wilder smoothing (مطابقة TradingView) |
| MACD | 12/26/9 | EMA قياسي + Histogram |
| VWAP | جلسة اليوم (Intraday) | يُحسب فقط عند توفر Intraday؛ وإلا يُعلَّم غير متاح |
| ATR | 14 فترة | Wilder — ويُشتق منه ATR% = ATR/Close |
| المتوسطات | SMA/EMA 20, 50, 200 | + موقع السعر نسبةً لكل متوسط |
| Bollinger | 20, 2σ | + عرض النطاق (Bandwidth) كمقياس انضغاط |

## 10.2 الدعوم والمقاومات (levels.py)

خوارزمية من مرحلتين: (1) استخراج القمم/القيعان المحلية (Local Extrema) على نافذة متحركة من بيانات 6–12 شهراً، (2) تجميع المستويات المتقاربة (ضمن 0.5×ATR) بـ Clustering بسيط، وترجيح كل مستوى بعدد اللمسات وحداثتها والحجم عندها. الناتج: أقوى 2–3 دعوم و2–3 مقاومات مع **درجة قوة معلنة لكل مستوى** (عدد اللمسات، آخر لمسة، الحجم).

## 10.3 التقلب (volatility.py)

- Historical Volatility: الانحراف المعياري السنوي للعوائد اللوغاريتمية على 20 و60 يوم.
- ATR% الحالي مقارنة بمتوسطه على 90 يوم (هل التقلب الحالي مرتفع نسبياً؟).

## 10.4 السيولة (liquidity.py)

- متوسط الحجم 20 يوم · الحجم النسبي RVOL (حجم اليوم ÷ المتوسط) · Dollar Volume (الحجم × السعر) · الـ Spread% من الـ Quote عند توفره.

## 10.5 تصنيف حالة السوق (regime.py)

قواعد رقمية معلنة (أمثلة قابلة للمعايرة أثناء التنفيذ):

```
trending_up:   Close > EMA50 > EMA200 و ميل EMA50 موجب و ADX(14) > 20
trending_down: عكس ما سبق
high_vol:      ATR% الحالي > 1.5 × متوسط ATR% (90 يوم)  ← تتقدم على غيرها
ranging:       ما لم ينطبق ما سبق
```

## 10.6 المخرج الموحّد

كل الوحدات تصب في JSON واحد موثّق بـ Pydantic Schema صارم (أنواع، حدود، وحدات). هذا الـ JSON هو **العقد الوحيد** بين المحرك الحتمي وكل ما بعده (الدرجات، الـ LLM، قاعدة البيانات).

---

# 11. محرك تحليل الأوبشن — Options Analysis Engine (المرحلة 2)

يُوثَّق الآن ليُبنى بعد استقرار محرك الأسهم. نفس القاعدة: حساب رياضي 100%.

## 11.1 الحسابات

| العنصر | الطريقة |
|--------|---------|
| Greeks (Delta, Gamma, Theta, Vega) | من المزوّد إن توفرت، وإلا حساب Black-Scholes عبر py_vollib بمدخلات (السعر، الإضراب، الوقت للانتهاء، IV، سعر الفائدة الخالي من المخاطرة) |
| IV Rank / Percentile | ترتيب IV الحالي ضمن مداه على 252 جلسة |
| Spread% | (Ask − Bid) ÷ Mid — مقياس جودة التنفيذ الأهم |
| Expected Move | Straddle الأقرب للـ ATM × 0.85 (تقريب معياري) أو IV×السعر×√(الأيام/365) |
| سيولة العقد | Volume + OI + Spread% مجتمعة |

## 11.2 ترتيب العقود (contract_ranker.py)

معادلة جودة معلنة لكل عقد (Contract Quality Score) توزن: السيولة (35%)، معقولية الـ Spread (25%)، توافق Delta مع الأفق الزمني المطلوب (20%)، وضع IV Rank (20%). النظام **لا يقول "اشترِ هذا العقد"** — يقول "هذا العقد الأعلى جودةً بنيوياً ضمن السلسلة، وهذه أرقامه مقارنةً بالبدائل".

---

# 12. نظام الدرجات — Scoring System (معادلات معلنة)

**كل درجة = معادلة رياضية شفافة. الـ LLM يستقبل الدرجات جاهزة ويُمنع من توليد أي درجة.**
كل المعادلات التالية قابلة للمعايرة أثناء التنفيذ، لكن مبدأ "معادلة معلنة قابلة للعرض في الواجهة" ثابت.

## 12.1 Technical Score (0–100)

مجموع موزون لمكونات ثنائية/متدرجة:

```
TS = 20×(اتجاه المتوسطات: Close>EMA20>EMA50>EMA200 متدرجة)
   + 15×(موقع RSI: 45–65 صحي=1، 30–45 أو 65–75=0.5، تطرف=0)
   + 15×(MACD: Histogram موجب ومتزايد=1، موجب متناقص=0.5، سالب=0)
   + 20×(قرب السعر من دعم قوي مقابل بعده عن مقاومة: نسبة المسافتين)
   + 15×(RVOL بين 0.8–2.5 = صحي)
   + 15×(بنية القمم والقيعان: HH/HL=1، LL/LH=0، مختلط=0.5)
```

## 12.2 Volatility Score (0–100)

`VS = 100 × النسبة المئوية لترتيب ATR% الحالي ضمن آخر 90 جلسة` — درجة عالية = تقلب مرتفع تاريخياً (معلومة محايدة، ليست جيدة أو سيئة بذاتها).

## 12.3 Liquidity Score (0–100)

```
LS = 40×min(DollarVolume/50M, 1) + 30×min(AvgVol20/1M, 1)
   + 30×(1 − min(Spread%/0.5%, 1))
```

## 12.4 Risk Score (0–100، الأعلى = أخطر)

```
RS = 35×(VS/100)
   + 25×(المسافة لمستوى الإبطال ÷ ATR: قريب جداً أو بعيد جداً = أخطر)
   + 20×(1 − LS/100)
   + 20×(قرب حدث معلوم: أرباح خلال ≤7 جلسات = 1)   ← يتطلب تقويم أرباح؛ في MVP يُعلَّم "غير مفحوص"
```

## 12.5 Overall Confidence (0–100)

```
OC = TS × (1 − RS/200) × جودة_البيانات
جودة_البيانات = 1.0 (لحظي) / 0.95 (مؤجل 15د) / 0.85 (يومي فقط)
```

**قاعدة عرض إلزامية:** أي درجة تُعرض في الواجهة أو التقرير تُرفق بإمكانية عرض معادلتها ومدخلاتها الفعلية ("لماذا 68؟").

---

# 13. محرك التقرير اللغوي — LLM Report Engine

## 13.1 الدور المحصور

الـ LLM **مترجم من الأرقام إلى اللغة** — لا محلل ولا حاسب. يستلم: JSON الحتمي + الدرجات + حالة السوق + المستويات، ويولّد: سرداً تفسيرياً منظماً بالعربية (أو الإنجليزية).

## 13.2 بنية الاستدعاء

- **استدعاء واحد** يولّد التقرير + فقرة Devil's Advocate معاً (دورين في Prompt واحد) — هذا أرخص من استدعاءين ويحقق الغرض في MVP. إن أثبت التدقيق لاحقاً أن جودة النقد تضعف بالدمج، يُفصل لاستدعاء ثانٍ صغير.
- الموديل: فئة اقتصادية سريعة (مثل Claude Haiku أو ما يعادله لدى OpenAI) — التقرير تفسيري ولا يحتاج أثقل موديل. الفئة قابلة للرفع بإعداد واحد.
- `max_tokens` مضبوط (~1500) والـ Prompt الثابت يُرسل مضغوطاً (بدون تكرار توثيق).

## 13.3 قواعد الـ Prompt الصارمة (تُكتب في System Prompt)

1. لا تخترع أي رقم غير موجود في الـ JSON.
2. لا تستخدم صيغة أمر تداولي ("اشترِ"، "ادخل الآن") — الصياغة تحليلية تعليمية فقط.
3. صرّح بعدم اليقين، واذكر ما لم يُفحص (مثل الأرباح في MVP).
4. التزم بالبنية: ملخص ← قراءة فنية ← مستويات ملحوظة ← سيناريوهات ثلاثة ← Devil's Advocate ← المخاطر ← "كيف وصلنا لهذه القراءة".

## 13.4 التحقق بعد التوليد (Numeric Validation)

خطوة كود إلزامية: استخراج كل الأرقام من نص التقرير ومطابقتها مع أرقام الـ JSON المصدر (مع تسامح تقريب). أي رقم دخيل → إعادة توليد مرة واحدة؛ فشل ثانٍ → إرجاع تقرير قالبي مبني بالكود من الـ JSON مباشرة (Fallback بلا LLM، تكلفة صفر، جودة لغوية أقل لكن دقة مضمونة).

---

# 14. المراجعة النقدية — Devil's Advocate / Risk Review

## 14.1 المبدأ

كل تقرير يتضمن **إلزامياً** فقرة تبحث عن أسباب ضد القراءة السائدة — لتقليل الانحياز التأكيدي.

## 14.2 مصدر المادة النقدية (حتمي أولاً)

المحرك الحتمي نفسه يجهّز "قائمة إنذارات" رقمية تُمرر للـ LLM، مثل: مقاومة قوية على بعد أقل من 1×ATR · RSI في تطرف · RVOL هابط أثناء صعود السعر (Divergence حجمي) · السعر ممدود عن EMA20 بأكثر من 2×ATR · Risk Score مرتفع · سيولة ضعيفة · تقلب منضغط قابل للانفجار (Bollinger Bandwidth في أدنى 10%). الـ LLM يصيغ هذه الإنذارات نقدياً — لا يخترعها.

## 14.3 قاعدة التوازن

إذا كانت قائمة الإنذارات فارغة، تقول الفقرة صراحةً: "لم يرصد المحرك إنذارات بنيوية بارزة — وهذا بحد ذاته يستدعي الحذر من الرضا الزائد" بدلاً من اختلاق نقد وهمي.

---

# 15. نظام التدقيق الذاتي — Self-Audit System

## 15.1 الهدف

الإجابة كمّياً على: "هل قراءات المنصة تتحقق فعلاً؟" — لكل حالة سوق ولكل أفق زمني.

## 15.2 الآلية

1. **عند كل تحليل:** تُستخرج المستويات القابلة للتحقق تلقائياً إلى `audit_targets` (دعوم، مقاومات، مستوى الإبطال، حدود السيناريوهات).
2. **وظيفة مجدولة يومية:** تفحص التحليلات التي مضى عليها 5 ثم 10 ثم 20 جلسة تداول، تجلب المسار السعري الفعلي منذ التحليل (High/Low/Close)، وتحدد: أي سيناريو تحقق؟ أي مستويات لُمست؟ هل ضُرب الإبطال قبل تحقق السيناريو؟
3. **Outcome Score (0–1):** معادلة معلنة — مثال للمعايرة: تحقق السيناريو الأرجح دون لمس الإبطال = 1.0 · تحقق جزئي = 0.5–0.8 · ضُرب الإبطال أولاً = 0.0–0.2 · حياد في سوق Ranging متوقَّع = 0.7.
4. **التجميع:** تحديث `accuracy_stats` حسب (حالة السوق × الأفق) — فيظهر مثلاً: "قراءات trending_up على أفق 10 جلسات: متوسط 0.71 من 34 تدقيقاً".

## 15.3 في MVP

الجداول كاملة + استخراج `audit_targets` يعمل من اليوم الأول + هيكل الوظيفة المجدولة موجود كـ Placeholder موثّق (تشغيلها الفعلي في الأسبوع التالي لاستقرار المحرك). **لا يُقبل تأجيل الجداول نفسها** — البيانات التي لا تُحفظ اليوم لا يمكن تدقيقها غداً.

## 15.4 الاستفادة المستقبلية

سجل الدقة يغذّي لاحقاً: معايرة أوزان الدرجات، وتحذير المستخدم تلقائياً ("دقة النظام التاريخية في حالة high_vol منخفضة: 0.41").

---

# 16. نظام التحكم بالتكلفة — Cost Control System

## 16.1 المكونات

| المكوّن | الوظيفة |
|---------|---------|
| **Cost Gate (وقائي)** | قبل أي استدعاء مدفوع: التكلفة التقديرية + المصروف الفعلي اليومي ≤ السقف؟ لا → رفض فوري برسالة واضحة، لا استدعاء |
| **Kill-Switch يدوي** | مفتاح في قاعدة البيانات + الواجهة يوقف كل الاستدعاءات المدفوعة فوراً مهما كان الرصيد |
| **Kill-Switch تلقائي** | يُفعَّل ذاتياً عند: بلوغ السقف اليومي/الشهري، أو رصد نمط شاذ (>10 استدعاءات LLM في دقيقة = خلل بالكود) |
| **cost_ledger** | تسجيل تقديري قبل + فعلي بعد لكل استدعاء (القسم 7) |
| **عدّاد الواجهة** | المصروف اليومي/الشهري ظاهر دائماً بنسبة مئوية من السقف |
| **تنبيه 80%** | رسالة تحذير في الواجهة (وبريد اختياري لاحقاً) عند بلوغ 80% من أي سقف |

## 16.2 القيم الافتراضية

يومي 1.00$ · شهري 20.00$ · قابلة للتعديل من الواجهة بدون نشر جديد. مع كاش فعّال وتقرير بـ ~0.02$، السقف اليومي يسمح بـ ~50 تحليلاً جديداً — أكثر من كافٍ لاستخدام شخصي.

## 16.3 درس Replit مقنّن

القاعدة المعمارية: **لا يوجد مسار كود يصل لاستدعاء مدفوع دون المرور بالـ Cost Gate.** تُفرض بالتصميم: الـ Adapters لا تُستدعى مباشرة، فقط عبر الـ Orchestrator الذي يبدأ بالبوابة.

---

# 17. نظام الكاش — Caching System

## 17.1 السياسة

| نوع البيانات | مدة الصلاحية (TTL) |
|--------------|---------------------|
| تقرير كامل — السوق مفتوح | 15 دقيقة |
| تقرير كامل — السوق مغلق | حتى الافتتاح التالي (لا جديد يستدعي إعادة الحساب) |
| OHLCV يومي تاريخي | 24 ساعة (التاريخ لا يتغير) |
| Quote لحظي | 60 ثانية |
| سلسلة أوبشن (مرحلة 2) | 10 دقائق خلال السوق |

## 17.2 التنفيذ

MVP يبدأ بـ **جدول كاش داخل Postgres** (أعمدة: key, value JSONB, expires_at) — يوفّر خدمة Redis منفصلة وتكلفتها. الواجهة البرمجية للكاش مجردة (`CacheAdapter`) بحيث يُستبدل بـ Redis لاحقاً بلا تعديل خارج الـ Adapter إذا كبر الحمل.

## 17.3 قاعدة الشفافية

كل تقرير مُرجع من الكاش يحمل شارة "محدّث قبل X دقيقة" — لا يُوهَم المستخدم أنه تحليل لحظي جديد.

---

# 18. تصميم الأمان — Security Design

| المجال | الإجراء |
|--------|---------|
| مفاتيح API الخارجية (بيانات + LLM) | متغيرات بيئة فقط (env vars) في منصة الاستضافة — لا تُكتب في الكود ولا قاعدة البيانات ولا Git أبداً |
| الوصول للمنصة | `X-API-Key` طويل عشوائي للمرحلة الشخصية؛ عند فتحها لمجموعة: مصادقة جلسات + كلمات مرور مجزأة (bcrypt/argon2) ومفتاح لكل مستخدم |
| النقل | HTTPS إلزامي (توفره منصة الاستضافة تلقائياً) |
| المدخلات | Pydantic Validation على كل نقطة API + Regex صارم للرموز (يمنع الحقن) |
| Rate Limiting | حد استدعاءات لكل مفتاح (مثلاً 30/دقيقة) — حماية إضافية فوق الـ Cost Gate |
| قاعدة البيانات | اتصال SSL · مستخدم بصلاحيات محدودة · نسخ احتياطي يومي (توفره Render/المنصة) |
| السجلات | لا تُسجَّل المفاتيح أو الأسرار في الـ Logs إطلاقاً (فلتر تنقية) |
| التبعيات | تثبيت الإصدارات (lock file) + فحص ثغرات دوري (pip-audit) |

---

# 19. الطبقة القانونية — Legal / Disclaimer Layer

## 19.1 مبدأ الصياغة (مطبّق في كل النظام)

| ممنوع في المخرجات | البديل المعتمد |
|-------------------|----------------|
| "اشترِ الآن" / "ادخل عند 128" | "مستوى فني ملحوظ عند 128 وفق منهجية الدعوم المعلنة" |
| "توصية" / "Recommendation" | "قراءة تحليلية" / "Analytical Reading" |
| "وقف خسارتك عند X" | "مستوى الإبطال الفني للسيناريو الصاعد عند X" |
| "الهدف الأول" | "مقاومة/منطقة اهتمام فنية أولى" |
| أي وعد أو احتمال ربح مؤكد | تصريح دائم بعدم اليقين |

هذه القواعد **مفروضة تقنياً** في: System Prompt (قاعدة 13.3-2) + فلتر كلمات محظورة بعد التوليد + نصوص الواجهة.

## 19.2 إخلاء المسؤولية الثابت

نص عربي/إنجليزي يظهر أسفل **كل** تقرير وفي أول استخدام (موافقة صريحة تُحفظ بتاريخها): المنصة أداة تحليل تعليمية لدعم القرار، ليست مستشاراً مالياً مرخصاً، لا تقدم توصيات استثمارية، التداول بالأسهم والأوبشن ينطوي على مخاطر خسارة رأس المال كاملاً، والقرار ومسؤوليته على المستخدم وحده.

## 19.3 عند التوسع لمجموعة

قبل إتاحة المنصة لأي شخص آخر (حتى مجاناً): مراجعة متطلبات هيئة السوق المالية السعودية بخصوص التحليل المالي الموجه للجمهور، وإبقاء المجموعة مغلقة بدعوات مع موافقة موثقة على الشروط لكل مستخدم. (المالك مستشار مالي — تُتخذ هذه المراجعة بمعرفته وتُوثّق.)

---

# 20. خارطة طريق التطوير — Development Roadmap

| المرحلة | المدة التقديرية | التسليمات | معيار الانتقال |
|---------|-----------------|-----------|-----------------|
| **M0 — الأساسات** | أسبوع | هيكل المشروع · قاعدة البيانات كاملة · Adapters (بيانات + LLM) · Cost Gate + Ledger · الكاش | اختبارات الوحدات تمر · استدعاء بيانات ناجح ضمن السقف |
| **M1 — المحرك الحتمي** | 1–2 أسبوع | كل مؤشرات القسم 10 · الدعوم/المقاومات · حالة السوق · كل معادلات الدرجات · JSON Schema | مطابقة المؤشرات مع TradingView على 5 رموز |
| **M2 — التقرير والواجهة** | أسبوع | LLM Report + Devil's Advocate + Validation الرقمي · واجهة الصفحة الواحدة RTL · عدّاد التكلفة | تقرير كامل ≤15 ثانية بتكلفة ≤0.05$ |
| **M3 — Self-Audit تشغيلي** | أسبوع | الوظيفة المجدولة تعمل · accuracy_stats يتحدث · صفحة الدقة | أول دفعة تدقيق فعلية بعد 5 جلسات |
| **M4 — التشغيل التجريبي** | 2–4 أسابيع استخدام | استخدام يومي حقيقي · معايرة الأوزان · إصلاحات | قرار بالانتقال للأوبشن |
| **M5 — محرك الأوبشن** | 2 أسبوع | القسم 11 كاملاً + مزوّد بيانات الأوبشن | جودة Greeks مطابقة لمنصة وسيط مرجعية |
| **M6+ — المستقبل** | حسب الجدوى | القسم 22 | يقرره سجل الـ Self-Audit والاستخدام |

**قاعدة حاكمة:** لا تبدأ مرحلة قبل اكتمال معيار الانتقال للتي قبلها.

---

# 21. نطاق MVP — التعريف الدقيق

## داخل MVP (وفق طلب المالك حرفياً)

إدخال رمز (NVDA/TSLA/AAPL...) · بيانات السعر · المؤشرات الفنية · الدعوم/المقاومات · التقلب · السيولة · حالة السوق · مستوى المخاطرة · درجات حتمية بمعادلات معلنة · تقرير LLM نهائي واحد · حفظ كل تحليل بقاعدة البيانات · هيكل Self-Audit مع Placeholder للوظيفة المجدولة · نظام تحكم كامل بالتكلفة مع Kill-Switch · كاش · واجهة صفحة واحدة · الطبقة القانونية.

## خارج MVP صراحةً

الأوبشن (M5) · تحليل الصور · الأخبار والمشاعر · Options Flow اللحظي · Dark Pool · Insider · تعدد مستخدمين كامل · تطبيق جوال.

---

# 22. الميزات المستقبلية — Future Features (مرتبة بالأولوية المقترحة)

1. **محرك الأوبشن الكامل** (M5 — الأقرب).
2. **تقويم الأرباح والأحداث** — يرفع جودة Risk Score (المكوّن المعلَّم "غير مفحوص" في MVP).
3. **تحليل الصور** (TradingView/سلاسل أوبشن) عبر نموذج رؤية — بعد ثبات النواة.
4. **محرك الأخبار** بتلخيص رخيص + وسم تأثير تقديري غير يقيني.
5. **المشاعر** (Retail vs Professional) كإشارة إضافية معلَّمة بعدم اليقين.
6. **معايرة الأوزان آلياً** من سجل Self-Audit (تحسين ذاتي مقنّن وشفاف).
7. **Options Flow لحظي + Unusual Activity** — فقط بعد أن يثبت الـ MVP جدواه (شرط المالك).
8. **تنبيهات** (بريد/تيليجرام) عند اقتراب السعر من مستويات محفوظة.
9. **مجموعة مغلقة صغيرة** — بعد استيفاء القسم 19.3.

---

# 23. خطة الاختبار — Testing Plan

| المستوى | النطاق | الأداة/الطريقة |
|---------|--------|-----------------|
| Unit Tests | كل دالة مؤشرات ودرجات — مقارنة بقيم مرجعية محسوبة يدوياً وبقيم TradingView | pytest + بيانات ثابتة (Fixtures) |
| Golden Files | JSON حتمي كامل لرموز مرجعية بتواريخ مجمّدة — أي تغيير غير مقصود بالنتائج يفشل الاختبار | pytest snapshot |
| Adapter Tests | كل مزوّد: نجاح، فشل، بيانات ناقصة، Timeout، التحويل للاحتياطي | Mocks + اختبار تكامل حي محدود |
| Cost Gate Tests | حالات: تحت السقف، عنده، فوقه، Kill-Switch يدوي، النمط الشاذ | pytest — **تغطية 100% إلزامية لهذه الوحدة** |
| LLM Validation Tests | تقارير فيها أرقام دخيلة مصطنعة → يجب رفضها؛ Fallback القالبي يعمل | Mocks |
| E2E | التدفق الكامل من الرمز للتقرير على بيئة Staging | httpx + سيناريوهات |
| Self-Audit Tests | بيانات تاريخية مصطنعة بنتائج معروفة → Outcome Score المتوقع | pytest |
| اختبار قانوني آلي | فلتر الكلمات المحظورة (القسم 19.1) على كل تقرير مولّد في الاختبارات | قائمة كلمات + CI |

CI بسيط (GitHub Actions): الاختبارات + pip-audit عند كل Push، والنشر لا يتم إلا بنجاحها.

---

# 24. خطة النشر — Deployment Plan

## 24.1 البيئات

| البيئة | المكان | الغرض |
|--------|--------|-------|
| تطوير | جهاز محلي (SQLite + مفاتيح تجريبية) | البناء اليومي |
| إنتاج | **Render** — Web Service (خطة Hobby) + Managed PostgreSQL | الاستخدام الفعلي |

اختيار Render مبني على نقاش سابق: فوترة بالخطة لا بالرصيد (الخدمة لا تتوقف فجأة — عكس تجربة Replit)، Cron Jobs مدمجة للـ Self-Audit، ونسخ احتياطي للقاعدة. المعمارية (Docker + Adapters) تُبقي الانتقال لأي منصة أخرى ممكناً بلا تعديل كود.

## 24.2 آلية النشر

GitHub هو المصدر الوحيد للحقيقة → Render Auto-Deploy من فرع main بعد نجاح CI → Health Check `/api/v1/health` بعد كل نشر → Rollback بنقرة من لوحة Render عند أي مشكلة. المفاتيح كلها في Render Environment Variables.

## 24.3 التكلفة التشغيلية الشهرية التقديرية (مرحلة MVP)

| البند | التقدير |
|------|---------|
| استضافة Web Service | 0–7$ |
| PostgreSQL مُدارة | 0–7$ (الخطط الأساسية) |
| بيانات السوق (مؤجلة) | 0–10$ |
| LLM (بالاستخدام الشخصي مع الكاش) | 1–5$ |
| **الإجمالي** | **~2–25$ / شهر** (مقابل 100$ كريدت واحدة في التجربة السابقة) |

---

# 25. التكلفة التقديرية لكل تحليل — Estimated Cost per Analysis

## 25.1 تفكيك التكلفة (تحليل سهم واحد، بدون كاش)

| المكوّن | التقدير | ملاحظات |
|---------|---------|---------|
| بيانات السوق | ~0.000–0.002$ | ضمن اشتراك شهري ثابت غالباً → التكلفة الحدية شبه صفرية |
| المحرك الحتمي | 0.000$ | Python محلي — حوسبة الاستضافة الثابتة |
| LLM: مدخلات (~3–4K توكن: JSON + Prompt مضغوط) | ~0.003–0.005$ | فئة اقتصادية (Haiku أو معادلها) |
| LLM: مخرجات (~1.2–1.5K توكن تقرير عربي) | ~0.006–0.008$ | max_tokens مضبوط |
| **الإجمالي لكل تقرير جديد** | **~0.01–0.02$** | الهدف المعتمد ≤0.05$ محقق بهامش |
| تقرير من الكاش | **0.000$** | متوقع 30–50% من الطلبات الفعلية |

## 25.2 سيناريو استخدام شهري واقعي

20 تحليلاً جديداً يومياً × 22 يوم تداول × 0.02$ = **~8.8$ شهرياً LLM كحد أعلى** — وواقعياً أقل مع الكاش. المرحلة 2 (أوبشن) تضيف تقديرياً 50–100% على تكلفة التقرير (JSON أكبر) وتبقى ضمن السقف.

## 25.3 مقارنة مع النهج السابق (14 وكيل LLM)

| | النهج السابق (Replit) | هذه المعمارية |
|--|----------------------|----------------|
| استدعاءات LLM لكل تقرير | 14 | 1 |
| تكلفة تقديرية للتقرير | 0.5–2$ | 0.01–0.02$ |
| مخاطر أرقام مهلوسة | عالية (LLM يحسب) | شبه معدومة (كود يحسب + Validation) |
| زمن التقرير | عشرات الثواني | ≤15 ثانية |
| **خفض التكلفة** | — | **~97%** |

---

# ملحق أ — قرارات مفتوحة تُحسم عند بدء التنفيذ (بموافقة المالك)

1. اختيار مزوّد البيانات الأساسي والاحتياطي بالأسعار السارية وقتها (وفق معايير القسم 9.3).
2. React خفيف أم قوالب Server-Rendered للواجهة (القسم 6.1).
3. معايرة الأوزان النهائية لمعادلات الدرجات على بيانات حقيقية (القيم الواردة نقطة بداية موثقة).
4. نصوص إخلاء المسؤولية النهائية (يراجعها المالك بصفته المهنية).

# ملحق ب — شروط الاعتماد

هذه الوثيقة تصبح مرجعاً تعاقدياً للتنفيذ بعد موافقة المالك. أي تعديل لاحق على النطاق يُوثّق كنسخة جديدة (1.1، 1.2...) مع سبب التغيير.

**— نهاية الوثيقة — بانتظار الاعتماد قبل كتابة أي كود —**

---

# ملحق ج — سجل التغييرات: النسخة 1.1 (معتمدة من المالك)

بناءً على قرارات المالك بتاريخ يوليو 2026:

1. **مسار بناء متوازٍ:** بعد اكتمال M0 (الأساسات المشتركة)، يُبنى محرك الأسهم (M1–M2) بالتوازي مع وحدة "صورة العقد ← مستويات الخروج" (كانت مؤجلة للمرحلة 3، أصبحت M2-B).
2. **وحدة M2-B — تحليل صورة عقد الأوبشن:** رفع صورة عقد (وسيط/TradingView) ← نموذج رؤية يستخرج (الرمز، Call/Put، الإضراب، الانتهاء، سعر العقد) ← جلب بيانات السهم الأم ← حساب حتمي: مستوى الإبطال الفني، Expected Move، ترجمة مستويات السهم لسعر العقد عبر Delta/Gamma (py_vollib)، وتآكل Theta اليومي ← تقرير مستويات خروج وهدف بصياغة "مستويات فنية" (القسم 19). ملاحظة: استدعاء الرؤية يُحتسب في Cost Ledger كأي استدعاء LLM.
3. **وحدة SMC (ضمن M1):** ثلاث خوارزميات حتمية إضافية: كشف RSI Divergence (توافق/تعارض القمم والقيعان بين السعر والمؤشر)، كشف Order Blocks (آخر شمعة عكسية قبل حركة اندفاعية > 2×ATR مع حجم أعلى من المتوسط)، ومؤشر Accumulation/Distribution التراكمي مع ميله.
4. **قرار بيانات نهائي:** ملف المستخدم = احتفاظ بالعقود لساعات (Intraday Swing). المعتمد: أسهم لحظية IEX (مستوى مجاني مثل Alpaca) + سلاسل أوبشن مؤجلة 15 دقيقة. الترقية للتدفق اللحظي الكامل مشروطة بنتائج Self-Audit (بند 22-7).
5. **مؤشرات سيولة موسعة (ضمن M1):** Unusual Volume (RVOL > 2.5 مع اتجاه)، وتغيرات OI اليومية للأوبشن في M5.

**— نهاية النسخة 1.1 —**
