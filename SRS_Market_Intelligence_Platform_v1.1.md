# SRS v2 — منصة فرص الأسهم الأمريكية منخفضة السعر

## النطاق

منصة عربية RTL لتحليل الأسهم الأمريكية المدرجة في بورصات رئيسية. تحليل السهم المحدد لا يخضع لنطاق سعري، بينما يتيح ماسح السوق نطاقًا اختياريًا افتراضيًا من 2 إلى 10 دولارات أو جميع الأسعار. المنصة لا تنفذ أوامر ولا تضمن الربح.

## حالات النتيجة

`conditional_entry`, `watch`, `wait_confirmation`, `expired`, `no_trade`.

## المعمارية

- `app/providers`: عقد موحد قابل للتبديل لبيانات السوق.
- `app/opportunities/quality.py`: رفض البيانات القديمة أو الناقصة والسبريد المرتفع.
- `app/opportunities/indicators.py`: حسابات فنية حتمية.
- `app/opportunities/strategies.py`: سجل الاستراتيجيات المستقلة وحالة لا صفقة.
- `app/opportunities/risk.py`: العائد/المخاطرة وحجم الصفقة.
- `app/opportunities/jobs.py`: المسح في الخلفية.
- `app/stocks/analysis.py`: تحليل سهم واحد كامل دون فلاتر استبعاد الماسح.
- `app/opportunities/openai_review.py`: مراجعة OpenAI المهيكلة بعد الفلترة المحلية.
- `app/api/routes_opportunities.py`: بدء المهام والاستعلام والنتائج والإعدادات.

## البيانات والجودة

تعرض النتيجة آخر صفقة وBid وAsk وMid/Spread والمصدر ونوع الخلاصة وعمر السعر. يرفض التحليل عندما يتجاوز عمر السعر الحد، أو يغيب Bid/Ask، أو يتجاوز السبريد الحد. يظهر تحذير IEX لأنه ليس SIP كاملًا.

## الاستراتيجيات

اختراق بحجم، استعادة VWAP، ارتداد دعم، استمرار اتجاه، Gap and Go، اختراق نطاق الافتتاح، انعكاس مبالغ فيه، ولا صفقة. كل دخول مشروط بتأكيد شمعة وحجم وبصلاحية قصيرة.

## الذكاء الاصطناعي

OpenAI فقط عبر Responses API وStructured Outputs. لا يجلب النموذج بيانات ولا يحسب مؤشرًا أو مستوى. يراجع أفضل 5–10 مرشحين فقط، ولا يعاد التحليل عند تطابق بصمة البيانات.

## التدقيق

كل فرصة تحفظ وقتها وسعرها ودخولها ووقفها وأهدافها وأحداثها. تحسب الإحصاءات من النتائج المسجلة فقط، ولا تسمى Backtest.

## الأمان

الأسرار في بيئة الخادم فقط. يوجد Rate Limit وميزانية يومية وسجل تكلفة. الأخبار نص غير موثوق ومعزول عن تعليمات النظام.
