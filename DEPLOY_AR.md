# دليل النشر على Render (بالعربي)

هذا الدليل يمشّيك خطوة بخطوة على نشر المنصة على Render حتى يطلع رابط عام، مع قفل
الدخول برمز وإخلاء المسؤولية الإلزامي.

---

## أولاً: رفع الكود على GitHub (مستودع خاص)

المستودع لازم يكون **Private (خاص)** — فيه منطق تحليلي وإعدادات، ومع إن المفاتيح لا
تُرفع أبداً (محمية بـ `.gitignore`)، الخصوصية مطلوبة.

### الطريقة أ — عبر سطر الأوامر (لو عندك gh):
```bash
gh repo create market-platform --private --source=. --remote=origin --push
```

### الطريقة ب — يدوياً (لو ما عندك gh):
1. افتح https://github.com/new
2. الاسم: `market-platform`
3. **اختر Private** (مهم جداً — لا تتركه Public)
4. **لا** تضف README أو .gitignore (المشروع فيه أصلاً)
5. اضغط Create repository
6. نفّذ هذه الأوامر من مجلد المشروع (استبدل `<USERNAME>` باسمك على GitHub):
```bash
git remote add origin https://github.com/<USERNAME>/market-platform.git
git push -u origin main
```

تأكيد النجاح: افتح رابط المستودع، ولازم تشوف الملفات، ويظهر بجانب الاسم شارة
🔒 **Private**. تأكد أن ملف `.env` **غير موجود** في القائمة (يجب ألا يظهر إطلاقاً).

---

## ثانياً: النشر على Render

### الطريقة الأسهل — Blueprint (ملف `render.yaml` جاهز في المشروع):

1. سجّل دخول على https://render.com (اربط حساب GitHub).
2. اضغط **New +** ← **Blueprint**.
3. اختر مستودع `market-platform`.
4. Render يقرأ `render.yaml` تلقائياً ويجهّز: خدمة ويب + قاعدة بيانات Postgres.
5. سيطلب منك تعبئة **المتغيرات السرية** (المعلَّمة `sync: false`) — انسخها من القائمة
   أدناه.
6. اضغط **Apply** — Render يبني ويشغّل ويطلّع لك رابطاً عاماً مثل
   `https://market-platform.onrender.com`.

### المتغيرات السرية اللي بتكتبها في لوحة Render بالضبط:

| المتغير | القيمة |
|---|---|
| `API_KEY` | نص عشوائي طويل (32+ حرف) — يوقّع كوكي القفل أيضاً. مثال يُولّد بأمر: `openssl rand -hex 32` |
| `ACCESS_CODE_MAIN` | رمز الدخول الرئيسي اللي بتوزعه (اختر شيئاً قوياً، مثل `Khaled-2026-xY7q`) |
| `ALPACA_API_KEY` | مفتاح Alpaca (من حساب Paper Trading) |
| `ALPACA_API_SECRET` | سر Alpaca |
| `ANTHROPIC_API_KEY` | مفتاح Anthropic (يبدأ بـ `sk-ant-`) |

> باقي المتغيرات (المزوّد، النماذج، سقوف التكلفة، قاعدة البيانات) معبّأة تلقائياً من
> `render.yaml` — لا تحتاج تكتبها.

### فحص الصحة (Health Check)
مضبوط تلقائياً على `/api/v1/health` — Render يستخدمه ليعرف أن الخدمة صحّية قبل توجيه
الزوّار.

### الهجرات (Migrations)
تُنفَّذ تلقائياً عند كل نشر عبر `alembic upgrade head` (جزء من `startCommand`)، فتُنشأ كل
الجداول — بما فيها جدول رموز الدخول — بدون تدخل يدوي.

---

## ثالثاً: بعد النشر

1. افتح الرابط العام — لازم تظهر **شاشة القفل** فوراً (رمز + مربع الموافقة على الإقرار).
2. ادخل بـ `ACCESS_CODE_MAIN` بعد تعليم مربع الموافقة.
3. لإعطاء شخص رمزاً خاصاً: افتح `/lock/admin` (يفتح فقط لحامل الرمز الرئيسي)، اكتب اسمه،
   واضغط "إنشاء رمز" — انسخ الرمز الظاهر وأرسله له (يظهر مرة واحدة فقط).
4. لإلغاء دخول شخص: من نفس الصفحة اضغط "إلغاء" أمام اسمه — يُقطع دخوله فوراً.

---

## ملاحظات مهمة

- **الخطة المجانية**: خدمة الويب المجانية "تنام" بعد فترة خمول، وأول طلب بعد النوم يكون
  بطيئاً (~30 ثانية). قاعدة Postgres المجانية تنتهي بعد 90 يوماً — رقّها لاحقاً لو أردت
  استمرارية.
- **الأسرار لا تُرفع أبداً**: `.env` في `.gitignore`، و`render.yaml` يحوي فقط أسماء
  المتغيرات لا قيمها.
- **المجدول (التدقيق الذاتي)** يعمل تلقائياً يومياً 01:00 UTC داخل الخدمة.
- **مفتاح API للبرمجة**: نداءات `/api/v1/*` التي تحمل ترويسة `X-API-Key` الصحيحة تتجاوز
  شاشة القفل (للاستخدام البرمجي)، أما المتصفح فيمرّ عبر القفل دائماً.
